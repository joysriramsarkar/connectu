/**
 * MongoDB connection — currently unused.
 *
 * ConnectU uses PostgreSQL (Neon) as the primary database.
 * This file is kept for reference but not imported anywhere.
 * It will be removed in a future cleanup once the MongoDB
 * dependency is confirmed to be unused.
 *
 * To re-enable: install the 'mongodb' package and uncomment below.
 */

// import { MongoClient } from 'mongodb';
// ... (connection code removed — not in use)

export {};